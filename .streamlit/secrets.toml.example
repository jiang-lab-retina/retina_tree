# Optional: protect the Admin review page with a password.
# Copy to `.streamlit/secrets.toml` for local use, or add in Streamlit Cloud Secrets.

[app]
admin_password = "change-me"

# Leave admin_password unset (or remove this file) to allow open access to Admin review.
